import { Cake } from "lucide-react"
import CakeForm from "../(components)/CakeForm"

function UploadCakes(){
    return(
        <section className="w-100 min-h-screen flex items-center justify-center">
            <CakeForm></CakeForm>
        </section>
    )
}

export default UploadCakes